# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Ralph runtime artifacts (these belong in the consuming project, not here)
prd.json
progress.md
runs/
archive/

# SuperStack runtime ledger (gitignored; override via SUPERSTACK_DIR)
.superstack/

# Subagent-driven-development process scratch (briefs, reports, diffs, progress)
.superpowers/

# Dependencies (if a project layers on top)
node_modules/
