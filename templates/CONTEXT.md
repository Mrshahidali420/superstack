# CONTEXT

> Long-term project memory: decisions and why, conventions to follow, and gotchas to avoid.
> Updated in the Learn phase whenever something is discovered that future work must respect.

## Decisions
<!-- Decision -> the why. e.g. "Postgres over SQLite — we need concurrent writers." -->

## Conventions
<!-- Patterns this codebase uses. e.g. "All API responses use the { data, error } envelope." -->

## Gotchas
<!-- Traps. e.g. "Updating a user also requires invalidating the session cache." -->
